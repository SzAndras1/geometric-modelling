/**
* Initialize the content of the canvas.
*/
function initContent()
{
  // let splinePanel = new SplinePanel();
  // screen.setRoot(splinePanel);
  // let bezierPanel = new BezierPanel();
  // screen.setRoot(bezierPanel);
  // let besselPanel = new BesselPanel();
  // screen.setRoot(besselPanel);
  let degreeIncreasePanel = new DegreeIncreasePanel();
  screen.setRoot(degreeIncreasePanel);
}

